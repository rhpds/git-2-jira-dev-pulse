apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - route.yaml
  - configmap.yaml
  - secret.yaml
  - pvc.yaml

commonLabels:
  app.kubernetes.io/part-of: devpulse
